# Homebrew environment
if [ -x "/opt/homebrew/bin/brew" ]; then
  eval "$(/opt/homebrew/bin/brew shellenv)"
fi

# Ensure unique entries in PATH
typeset -U path PATH

# PATH configuration (consistent with configs/fish/conf.d/00-paths.fish)
path=(
  "$HOME/.local/bin"
  $path
)
export PATH
