export LANG="zh_TW.UTF-8"
export COLORTERM=truecolor
export TERM=xterm-256color

setopt EXTENDED_GLOB

setopt MENU_COMPLETE
setopt NO_LIST_AMBIGUOUS

export PNPM_HOME="$HOME/Library/pnpm"

typeset -U path
typeset -T PATH path
path=(
    "$PNPM_HOME"
    /opt/homebrew/bin
    /opt/homebrew/sbin
    /opt/homebrew/opt/bash/bin
    /opt/homebrew/opt/zsh/bin
    /opt/homebrew/opt/curl/bin
    "$HOME/Developer/my-macos-build/cli"
    $path
)
export PATH

if [[ -d /opt/homebrew/share/zsh-completions ]]; then
  fpath=(/opt/homebrew/share/zsh-completions $fpath)
fi
fpath=(~/.zsh/completions $fpath)

autoload -Uz compinit

_comp_path="${ZDOTDIR:-$HOME}/.zcompdump"
if [[ -n "$_comp_path"(#qN.mh+24) ]]; then
    compinit
    { zcompile "$_comp_path" } &!
else
    compinit -C
fi

local custom_cli_dir="$HOME/Developer/my-macos-build/cli"
if [[ -d "$custom_cli_dir" ]]; then
    local -a my_cmds
    my_cmds=("$custom_cli_dir"/*(N:t))
    
    if (( ${#my_cmds[@]} > 0 )); then
        compdef _files "${my_cmds[@]}"
    fi
fi

zstyle ':completion:*' list-colors "${(s.:.)LS_COLORS}"
zstyle ':completion:*' menu select

zstyle ':completion:*' matcher-list 'm:{a-zA-Z}={A-Za-z}' 'r:|=*' 'l:|=* r:|=*'

zstyle ':completion:*' insert-tab pending

zstyle ':completion:*' completer _complete _ignored _files

zstyle ':completion:*:matches' group 'yes'
zstyle ':completion:*:options' description 'yes'
zstyle ':completion:*:options' auto-description '%z'
zstyle ':completion:*:descriptions' format '%F{green}-- %d --%f'
zstyle ':completion:*' group-name ''

bindkey '^[[A' history-search-backward
bindkey '^[[B' history-search-forward

autoload -Uz vcs_info

zstyle ':vcs_info:*' enable git
zstyle ':vcs_info:*' check-for-changes true
zstyle ':vcs_info:*' get-revision true

zstyle ':vcs_info:git:*' formats ' %F{cyan}(%b%c%u)%f'
zstyle ':vcs_info:git:*' actionformats ' %F{cyan}(%b|%a%c%u)%f'
zstyle ':vcs_info:git:*' stagedstr '%F{green}●%f'
zstyle ':vcs_info:git:*' unstagedstr '%F{red}●%f'

precmd_vcs_info() { vcs_info }
precmd_functions+=( precmd_vcs_info )

setopt PROMPT_SUBST
PROMPT='%F{green}%~%f${vcs_info_msg_0_} '

export ZSH_AUTOSUGGEST_HIGHLIGHT_STYLE="fg=#666666"

typeset -A ZSH_HIGHLIGHT_STYLES
ZSH_HIGHLIGHT_STYLES[default]='fg=#cccccc'
ZSH_HIGHLIGHT_STYLES[command]='fg=#4b8de1'
ZSH_HIGHLIGHT_STYLES[single-quoted-argument]='fg=#e5e510'
ZSH_HIGHLIGHT_STYLES[double-quoted-argument]='fg=#e5e510'
ZSH_HIGHLIGHT_STYLES[redirection]='fg=#11a8cd'
ZSH_HIGHLIGHT_STYLES[commandseparator]='fg=#bc3fbc'
ZSH_HIGHLIGHT_STYLES[unknown-token]='fg=#f14c4c'
ZSH_HIGHLIGHT_STYLES[path]='fg=#0dbc79'
ZSH_HIGHLIGHT_STYLES[comment]='fg=#666666'
ZSH_HIGHLIGHT_STYLES[assign]='fg=#29b8db'
ZSH_HIGHLIGHT_STYLES[history-search-backward]='bg=#276782'

ZSH_AUTOSUGGEST_PATH="/opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh"
if [[ -f "$ZSH_AUTOSUGGEST_PATH" ]]; then
    source "$ZSH_AUTOSUGGEST_PATH"
    [[ ! -f "${ZSH_AUTOSUGGEST_PATH}.zwc" ]] && { zcompile "$ZSH_AUTOSUGGEST_PATH" } &!
fi

ZSH_HIGHLIGHT_PATH="/opt/homebrew/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh"
if [[ -f "$ZSH_HIGHLIGHT_PATH" ]]; then
    source "$ZSH_HIGHLIGHT_PATH"
    [[ ! -f "${ZSH_HIGHLIGHT_PATH}.zwc" ]] && { zcompile "$ZSH_HIGHLIGHT_PATH" } &!
fi