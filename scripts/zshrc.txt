if type brew &>/dev/null; then
  FPATH=$(brew --prefix)/share/zsh/site-functions:${FPATH}
  zstyle ':completion:*' list-prompt ''
  zstyle ':completion:*' select-prompt ''
  zstyle ':completion:*' menu yes select
  autoload -Uz compinit
  compinit
fi

export CLICOLOR=1

alias ll='ls -lh'
alias la='ls -lah'

autoload -Uz vcs_info
precmd() { vcs_info }
zstyle ':vcs_info:git:*' formats '%F{6}( %b)%f'
setopt PROMPT_SUBST

PROMPT='%F{2}%~%f ${vcs_info_msg_0_}$ '

SCRIPT_HOME='~/Codespaces/my-macos-build'
SCRIPT_PATH="$SCRIPT_HOME/scripts"

alias mmb="code $SCRIPT_HOME"
alias mkgif="sh $SCRIPT_PATH/make-gif.sh"
alias ebk="sh $SCRIPT_PATH/extension-config-backup.sh"
alias urb="sh $SCRIPT_PATH/ublock-rule-backup.sh"
alias ua="sh $SCRIPT_PATH/update-all.sh"
alias rec="sh  $SCRIPT_PATH/re-encode.sh"
alias rsl='defaults write com.apple.dock ResetLaunchPad -bool true;killall Dock'
alias boxvpn='sudo /opt/homebrew/opt/openvpn/sbin/openvpn --config ~/.ssh/Box-Bear.ovpn'
alias boxrsync='cd ~ && rsync -ahPz --delete --exclude=.DS_Store /Volumes/BoxData/bear/Documents/ ~/Documents/ && chmod -R 700 ~/Documents'

source '/opt/homebrew/share/zsh-autosuggestions/zsh-autosuggestions.zsh'
source '/opt/homebrew/opt/zsh-fast-syntax-highlighting/share/zsh-fast-syntax-highlighting/fast-syntax-highlighting.plugin.zsh'

ZSH_AUTOSUGGEST_STRATEGY=(history completion)
